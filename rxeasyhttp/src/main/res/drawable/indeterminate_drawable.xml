<?xml version="1.0" encoding="UTF-8"?>
<rotate xmlns:android="http://schemas.android.com/apk/res/android"
    android:fromDegrees="0.0"
    android:pivotX="50.0%"
    android:pivotY="50.0%"
    android:toDegrees="360.0">

    <!--
android:thicknessRatio
它用圆环宽度的比率来表示圆环的厚度。
例如，如果android:thicknessRatio=”2”，那么厚度就等于圆环的宽度除以2。
这个值会被android:innerRadius覆盖。默认值是3。
android:innerRadiusRatio
它用圆环宽度的比率来表示内部圆环的半径。
例如，如果android:innerRadiusRatio=”5”，
那么内部半径就等于圆环的宽度除以5。
这个值会被android:innerRadius的值覆盖。默认是9。

    -->

    <shape
        android:innerRadiusRatio="2.3"
        android:shape="ring"
        android:thicknessRatio="15"
        android:useLevel="false">
        <gradient
            android:centerY="0.5"
            android:endColor="#006db110"
            android:startColor="#ffbb00"
            android:type="sweep"
            android:useLevel="false" />
    </shape>

</rotate>